autopep8==2.3.2
anthropic
blinker==1.9.0
chromadb
click==8.3.1
Flask==3.1.3
flask-limiter
requests>=2.32.0
google-genai
google-api-python-client
google-auth-oauthlib
gspread
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
openpyxl
Pillow
qrcode>=8.0
pycodestyle==2.14.0
python-dotenv==1.2.2
python-docx>=1.1.0
PyYAML>=6.0
reportlab
Werkzeug==3.1.7
pytest>=8.0.0
flask-cors>=4.0.0
opencv-python-headless
numpy

# --- Dependencias que el código importaba sin declarar (auditoría 20-sep-2026) ---
# Estaban instaladas a mano en el venv; sin esto el entorno no se podía reconstruir.
CairoSVG>=2.9.0                 # render SVG→PNG de etiquetas
weasyprint>=68.1                # HTML→PDF (fichas, catálogos)
PyPDF2>=3.0.1                   # manipulación de PDFs
pyzbar>=0.1.9                   # lectura de códigos de barras
cryptography>=46.0.5            # firma/cifrado
WooCommerce>=3.0.0              # API de la tienda
pywebpush>=2.3.0                # notificaciones push del panel
scrapling>=0.4.4                # scraping en knowledge_agent
openai>=2.24.0                  # cliente usado en un call-site puntual
tiktoken>=0.13.0                # conteo de tokens
fal-client>=0.13.2              # video Kling vía fal.ai
faster-whisper>=1.2.1           # STT local; usa ctranslate2, NO torch
ctranslate2>=4.7.2              # motor de faster-whisper + detección de GPU
soundfile>=0.13.1               # E/S de audio

# NO agregar `qwen-tts`: arrastra torch, gradio, transformers y accelerate
# (~7 GB de CUDA). El motor de voz vivo es voicebox, que corre en su propio
# venv (~/voicebox-venv, servicio voicebox-tts, puerto 17493).
